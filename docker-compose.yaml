services:
  subforge:
    build: .
    container_name: subforge
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - subforge-data:/data
    environment:
      - TZ=Asia/Shanghai

volumes:
  subforge-data:
